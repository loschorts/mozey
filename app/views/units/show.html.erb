<p id="notice"><%= notice %></p>

<h2>Bookings for Vehicle No: <%= @unit.serial %></h2>
<%= render partial: "bookings/bookings_for", locals: {object: @unit} %>

<%= link_to 'Edit', edit_unit_path(@unit) %> |
<%= link_to 'Back', units_path %>
